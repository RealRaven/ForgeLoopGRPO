# ForgeLoopGRPO — exact versions from verified environment
# Generated from pip freeze of working training environment
# CUDA 13.0, Python 3.11+

# ========== CORE ==========
torch==2.13.0
transformers==5.10.2
accelerate==1.13.0
numpy==2.3.5
pandas==3.0.3

# ========== GENERATION ==========
# vLLM for batched inference with LoRA hot-swap
# NOTE: LoRARequest API changed significantly across versions.
# This exact version was verified for the swap_manager generation loop.
vllm==0.25.1

# ========== LoRA & MODEL MERGING ==========
peft==0.19.1
safetensors==0.7.0

# ========== EMBEDDINGS (CPU semantic_diversity scoring) ==========
sentence-transformers==5.5.1

# ========== CONFIG VALIDATION ==========
pydantic==2.13.4

# ========== TRANSITIVE DEPS (pinned for stability) ==========
# These are pulled in automatically but pinned to prevent drift
scipy==1.17.1
scikit-learn==1.9.0
tqdm==4.68.1
filelock==3.29.1
fsspec==2026.4.0
requests==2.34.2
packaging==26.2
regex==2026.5.9
tokenizers==0.22.2
einops==0.8.2

# ========== OPTIONAL ==========
# datasets==5.0.0       # Uncomment if using HuggingFace datasets format